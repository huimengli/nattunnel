# 会话记录 — 2026-09-12 11:24

## 本次需求(用户原话)

"API通道也走 /root/Desktop/huimengli/nattunnel/server/app/static/nattunnel-client.exe,这样只要复制一次就行"

## 改动

- `server/app/routers/download.py`: `CLIENT_EXE` 由 `BASE_DIR.parent/"client"/"dist"/...` 改为
  `BASE_DIR/"app"/"static"/nattunnel-client.exe` — **两条下载通道现在读同一份文件**;
  404 提示语同步(指向 build.bat 自动同步/手工上传 app/static)。
- 服务器端不再需要 `client/dist/` — 部署只需 server/ 树。
- README API 表行、deploy-keliit-top.md §1(上传清单)/§6(第 4、6 条)同步为"唯一副本"表述。

## 验证

本地重启后 api_check **ALL PASS**(14 项): `GET /api/client/download -> 200` +
`GET /nattunnel-client.exe -> 200`, 两者同源 app/static/nattunnel-client.exe。

## 用户侧

keliit 服务器只需维护一份 exe: `/root/Desktop/huimengli/nattunnel/server/app/static/nattunnel-client.exe`;
旧的 `client/dist/` 副本可删可不删(服务端已不读)。
