# nginx 反代配置 —— 放在 /etc/nginx/conf.d/ 或 sites-enabled 内
# 作用: 把 www.xxx.com/tunnel/*、/api/* 与网页管理端 / 转发到本机 FastAPI(127.0.0.1:8000)
# 有证书时启用下面的 443 server 块; 没有证书可临时用 80 端口(ws/wss -> ws)。

server {
    listen 80;
    server_name www.xxx.com;

    # ---- 隧道 WebSocket(核心) ----
    location /tunnel/ {
        proxy_pass http://127.0.0.1:8000/tunnel/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        # 长连接, 空闲超时拉长(按业务调整)
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # ---- REST API ----
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ---- 网页管理端(登录 + 隧道/用户配置) ----
    location / {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# ================= 443 / TLS(推荐) =================
# server {
#     listen 443 ssl;
#     http2 on;
#     server_name www.xxx.com;
#
#     ssl_certificate     /etc/nginx/certs/www.xxx.com.pem;
#     ssl_certificate_key /etc/nginx/certs/www.xxx.com.key;
#
#     # location 段与上面 80 的完全相同(tunnel/api/webui 三段), 此处省略:
#     location /tunnel/ { ... }
#     location /api/    { ... }
#     location /        { ... }
# }
